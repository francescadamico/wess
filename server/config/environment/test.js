'use strict';

// Test specific configuration
// ===========================
module.exports = {
  // PostgreSQL connection options
  postgres: {
		user: 'postgres',
		password: 'passgres',
		host: 'localhost',
		port: '5432',
		database: 'postgres'
  }
};